name: Demonstration CI
on: push
jobs:
  init:
    runs-on: ubuntu-latest
    steps:
      - name: set up job
        run: echo "set up job"
      - name: etape 1 -Initialisation
        run : echo "Initialisation"
